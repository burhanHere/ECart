import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class CurrencyRatesService {
  constructor() {}

  getCurrencyRates(baseCurrency: number, TargetCurrency: number): number {
    
    
    return Number();
  }
}
